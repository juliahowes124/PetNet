// import { Photo } from './photo';

export interface Animal {
    id: number;
    name: string;
    photoUrl: string;
    // age: number;
    // gender: string;
    // created: Date;
    species: string;
    // photos?: Photo[];
}
